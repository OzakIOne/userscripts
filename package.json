{
  "name": "userscripts",
  "scripts": {
    "dev": "pnpm --stream -r run dev",
    "build": "pnpm --stream -r run build",
    "ts": "pnpm --stream -r run ts",
    "watch": "pnpm --stream -r run watch",
    "lint": "pnpm --stream -r run lint",
    "prettier": "pnpm --stream -r run prettier"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.41.0",
    "@typescript-eslint/parser": "^8.41.0",
    "eslint": "^9.34.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  },
  "packageManager": "pnpm@10.15.0",
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "24",
      "onFail": "download"
    }
  }
}
