{
  "name": "@userscripts/leboncoin",
  "version": "1.1",
  "description": "userscript on leboncoin to remove already selled items",
  "main": "src/leboncoin.user.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prettier": "prettier src/**/*.ts --write",
    "lint": "eslint src/**/*.ts --fix"
  }
}
